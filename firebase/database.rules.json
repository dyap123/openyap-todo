{
  "rules": {
    ".read": false,
    ".write": false,
    "todo": {
      ".read": "auth != null && auth.token.email === 'dyap123@gmail.com' && auth.token.email_verified === true",
      ".write": "auth != null && auth.token.email === 'dyap123@gmail.com' && auth.token.email_verified === true"
    }
  }
}
